WARNING: this data DOES NOT represents sugar DSL!

Here is only a bare tree, i.e. arbitrary dummy structure, which is NOT
meant to be any valid as tasks or so.

The only valid structure elements here is "import" and "exclude"
keywords as well as Jinja template syntax (or intentionally broken
one).
