language: python
python:
    - "2.7"
    - "3.3"
    - "3.4"
    - "3.5"
    - "3.6"
    - "3.7"

virtualenv:
  system_site_packages: true

env:
  - PYTHONPATH=./pylintplugins
install:
  - pip install -r requirements.txt

script:
  - pylint sugar/*
  - pytest
